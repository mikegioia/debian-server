Any nginx config files added to the sites-available directory will be added
as site configurations during the nginx install.

Any files added to this directory will be added to the root nginx config
directory.